import { FiltroPokedexPipe } from './filtro-pokedex.pipe';

describe('FiltroPokedexPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPokedexPipe();
    expect(pipe).toBeTruthy();
  });
});
